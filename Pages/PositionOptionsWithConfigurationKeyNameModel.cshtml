@page
@model PositionOptionsWithConfigurationKeyNameModel
@{
    ViewData["Title"] = nameof(PositionOptionsWithConfigurationKeyNameModel);
    Model.logger.LogInformation($"[{Model.Id}] {nameof(PositionOptionsWithConfigurationKeyNameModel)}.cshtml constructor called");

}

<h1 class="text-dark">@nameof(PositionOptionsWithConfigurationKeyNameModel)</h1>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}
<p><strong>Model.Id</strong>: <code>@Model.Id</code></p>
@* <p><strong>Model.RequestId</strong>: <code>@Model.RequestId</code></p> *@
<p><strong>Model.Message</strong>: <code>@Html.Raw(@Model.Message)</code></p>
